general {
    lock_cmd = pidof hyprlock || hyprlock       # avoid starting multiple hyprlock instances.
    before_sleep_cmd = loginctl lock-session    # lock before suspend.
    after_sleep_cmd = hyprctl dispatch dpms on  # to avoid having to press a key twice to turn on the display.
}

listener {
    timeout = 1800                                 # 30min
    # Wake the display first so hyprlock renders a frame and its update loop
    # starts (otherwise the clock freezes forever); then power the display
    # back off. Fixes frozen lockscreen clock when locking from an off screen.
    on-timeout = hyprctl dispatch dpms on && loginctl lock-session && sleep 3 && hyprctl dispatch dpms off
}

listener {
    timeout = 180                                 # 3min
    on-timeout = hyprctl dispatch dpms off        # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on          # screen on when activity is detected after timeout has fired.
}
